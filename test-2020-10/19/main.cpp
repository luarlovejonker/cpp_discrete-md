#include <iostream>
using namespace std;

template <class T>
T max(T x, T y)
{
  return (x > y) ? x : y;
}
int main()
{

  int i;
  char c;
  // max(i,i);
  // max(c,i);
  // max(i,i);
  // max(i,i);

}